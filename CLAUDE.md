# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

Backlog [0.1–2.5](specs/backlog_phase_1.md) done. Workspace skeleton (0.1–0.4), a NestJS scaffold in `apps/api` (1.1): global `ValidationPipe` (`whitelist`/`transform`), `/api/v1` prefix, `GET /health`, CORS locked to `CORS_ORIGIN` + `http://localhost:3000`, and the five flat D2 modules (`Prisma`, `Auth`, `Sets`, `UserSets`, `Coins` importing `Linking`) wired into `AppModule` as empty stubs awaiting their real logic in later tasks — plus `packages/shared` (1.2): `Denomination`/`Grade` enums and the SD §4 contract types (`UserSetSummary`, `GapViewResponse`/`GapSlot`, `CoinDto`, `CoinMutationResponse`/`SlotSuggestion`, `CoinLinkResponse`), built to `dist` via its own `tsc`; plus the Prisma schema (1.3): `apps/api/prisma/schema.prisma` — `User`, `CollectionSet`, `SetSlot` (`@@unique([setId, year, mintMark, label])`), `UserSet` (`@@unique([userId, setId])`), `CoinItem` (`@@unique([userId, slotId])`, `onDelete: SetNull` on the slot FK), plus a `PrismaService` (connect/disconnect lifecycle hooks) now exported from the previously-empty `PrismaModule` — not yet consumed by `apps/api` DTOs/services or `apps/web`. Neon project provisioned (1.4): `apps/api/.env` holds the real pooled `DATABASE_URL` (host has the `-pooler` suffix — Neon also hands out a direct/non-pooled host on the same project page that looks superficially the same; use the pooled one per SD §7), first migration (`prisma/migrations/20260714042946_init`) applied, five tables live in the `neondb` database. Swagger wired (1.5): `@nestjs/swagger`'s `DocumentBuilder`/`SwaggerModule` in `apps/api/src/main.ts`, UI + JSON at `/api/docs` and `/api/docs-json` (outside the `/api/v1` prefix — `SwaggerModule.setup` takes a raw path, not prefixed); `HealthController` carries the first `@ApiTags`/`@ApiOperation` decorators as the pattern to extend per-endpoint from here on, not as a later pass. `swagger-ui-dist` pulls in `@scarf/scarf` (a telemetry postinstall script) as a transitive dep — disallowed via `pnpm-workspace.yaml`'s `allowBuilds` rather than let it run. API deployed to Render (1.6): root `render.yaml` — `rootDir: apps/api`, build installs pnpm 11.5.0 then runs `prisma generate` + `pnpm --filter api build`, `healthCheckPath: /api/v1/health`, `DATABASE_URL`/`CORS_ORIGIN` set manually in the dashboard (`sync: false`), `JWT_SECRET` Render-generated; live at https://coin-collector-api.onrender.com, `/api/v1/health` returns `200 {"status":"ok"}`. Getting there took three follow-up fixes (pnpm version pin, `NODE_VERSION` 22.13.0, `prisma generate` added to `buildCommand`) — Render's default Node/pnpm didn't match the workspace's `engines` pin and the build didn't generate the Prisma client on its own, so re-check all three if a future Render redeploy fails at build time. Free-tier cold start after 15 min idle is expected (PRD §10), not a bug. Auth (2.1–2.5) is done: `RegisterDto`/`LoginDto` (`class-validator`, email trimmed+lowercased via `@Transform`, password `@MinLength(8)`); `AuthService.register` hashes with bcrypt cost 10 and turns the Prisma `P2002` unique-violation into a 409 (`ConflictException`) rather than checking existence first (avoids a TOCTOU gap); `AuthService.login` returns only `{ accessToken }` — the `{ sub, email }` pair lives inside the signed JWT payload, not as sibling fields in the response body, despite how the backlog line reads. `JwtModule` is HS256/7-day via `JwtModule.registerAsync` reading `JWT_SECRET` from `ConfigService`. `JwtAuthGuard` (wraps Passport's `AuthGuard('jwt')`, checks the `@Public()` reflector metadata) is registered as a global `APP_GUARD` in `AppModule` — every route needs either `@Public()` or a valid bearer token, not just the ones under `/auth`; `JwtStrategy.validate` maps the payload to `{ userId, email }` (note: `userId`, not `sub`) and that's what `@CurrentUser()` returns. Provider order in `AppModule` (`ThrottlerGuard` before `JwtAuthGuard`) is what makes SD D2's guard order real — Nest runs `APP_GUARD` entries in array order. Throttling (2.4) is two-tier: `ThrottlerModule.forRoot` sets a 100/min default globally, and `AuthController` overrides it to 5/min via `@Throttle({ default: { limit: 5, ttl: 60_000 } })` at the controller level, applying to both `/auth/register` and `/auth/login`. The 2.5 e2e test (`apps/api/test/auth.e2e-spec.ts`) boots the real `AppModule` but adds a throwaway `__test-protected` controller purely to have a non-`@Public()` route to hit — Sets/UserSets/Coins are still empty stubs with no real guarded endpoint yet, so this stand-in goes away once one exists; it cleans up its created user in `afterAll`. `apps/web` doesn't exist yet (still an empty dir — scaffold is 5.1), so no web-side `.env.example` yet either; `apps/api/.env.example` already covers the api side. Seed script (3.1) is done: `apps/api/prisma/seed.ts`, wired via the `prisma.seed` hook in `apps/api/package.json` (`ts-node prisma/seed.ts`, run through `prisma db seed`). Two-pass by design — every file under `seed/templates/*.json` is read and validated (denomination against the Prisma `Denomination` enum, slot shape, duplicate natural-key detection within a file) *before* any DB write, so one malformed file aborts the whole run rather than leaving a partial seed. `CollectionSet` has no DB-level unique constraint on `name` (schema unchanged from 1.3) — the script emulates "upsert by name" with `findFirst({ name, isTemplate: true })` + create-or-update inside a per-template `$transaction`, matching the SD §6 "one transaction per template" call. Slots are diffed in script code against the natural key `(year, mintMark, label)`, never via Prisma's own `upsert`, because Postgres NULL-distinctness makes the DB constraint advisory for Philadelphia (`mintMark: null`) slots; matched slots are updated in place (row id stable ⇒ `CoinItem.slotId` links survive), unmatched incoming slots are inserted, and existing slots absent from the JSON are `deleteMany`'d (relies on `onDelete: SetNull` on `CoinItem.slotId`, same as 1.3). Verified end-to-end against the real Neon dev DB with a throwaway fixture: create → idempotent re-run (0 created) → in-place update of `sortOrder`/`isKeyDate` with row ids unchanged → deletion of a removed slot → malformed-file abort, all confirmed, then the fixture and its DB rows were removed. One incidental fix: `apps/api/tsconfig.build.json` now excludes `prisma/` (same as it already excluded `test/`) — `nest build` was pulling `prisma/seed.ts` into the `rootDir: ./src` program and failing with TS6059 before this. Three templates authored and seeded (3.2): `seed/templates/lincoln-wheat-cents.json` (142 slots, 1909–1958, `denomination: Cent`), `state-quarters.json` (100 slots, 1999–2008, `denomination: Quarter`, `label` carries the state name since year+mint alone doesn't disambiguate the 5-per-year release), `morgan-dollars.json` (96 slots, 1878–1904 + 1921, `denomination: Dollar`); all three cross-checked against standard Red Book mint-mark-by-year tables, with VDB/No-D/Doubled-Die/Steel/Proof varieties disambiguated via `label` rather than a fourth natural-key field. Running the real seed against these surfaced a bug in the 3.1 script rather than in the templates: `$transaction` was using Prisma's default 5s interactive-transaction timeout, which the 142-row Lincoln Cents template (one network round trip per row against Neon) blew past — 3.1's own verification used a small throwaway fixture that never hit this ceiling. Fixed by passing `{ timeout: 30_000 }` as the second arg to `prisma.$transaction` in `apps/api/prisma/seed.ts`; re-verified end-to-end against the real Neon dev DB — first run created all 338 slots across the three templates, immediate re-run showed 0 created/0 deleted (existing-slot rows all matched and updated in place, confirming idempotency). Sets endpoints (3.3) are done: `SetsModule` (`apps/api/src/sets`) — `GET /sets` takes an optional `isTemplate` query bool (`FindSetsQueryDto`, string→bool via `@Transform` since query params arrive as strings even under the global `transform: true` pipe); omitted returns templates plus the caller's own custom sets in one query (`OR: [{isTemplate:true}, {isTemplate:false, ownerId:userId}]`), `isTemplate=false` scopes strictly to `ownerId`, matching the backlog's "non-templates scoped to owner." `POST /sets/:id/activate` 404s on an unknown set, 403s on someone else's non-template set, then `userSet.upsert` on the `(userId, setId)` compound key — upsert, not findFirst-then-create, so idempotent re-activation isn't a check-then-act race (same pattern as 2.1's register-conflict handling). Verified live against the seeded Neon dev DB: unauthenticated `GET /sets` → 401; authenticated → the three seeded templates; `isTemplate=false` → `[]` (no custom sets exist yet — that's Phase 2); activating a template twice returned the identical `UserSet` id/`activatedAt` both times; activating a bogus id → 404. Since `@CurrentUser()` needed a real decorated-parameter type import, `AuthenticatedUser` had to be imported as `import type` in the new controller — `isolatedModules`+`emitDecoratorMetadata` reject a value-style import of a type-only symbol used only in a decorated signature. Note: the 2.5 e2e test's `__test-protected` stand-in controller (`apps/api/test/auth.e2e-spec.ts`) is still in place — `GET /sets` is now a real non-`@Public()` route it could exercise instead, but swapping that over is bundled with 3.5's own e2e test, not done here. Also regenerated the Prisma client (`prisma generate`) before writing this module — the checked-in `node_modules` copy predated the 1.3 schema and had no `UserSet`/`CollectionSet` types at all; re-run that command if a fresh clone's Prisma types look similarly stale. Coins CRUD (4.2) is done: `CoinsModule` (`apps/api/src/coins`) — `GET/POST /coins`, `PATCH /coins/:id`, `DELETE /coins/:id`, all scoped to `@CurrentUser()`; PATCH/DELETE 404 on an unknown id, 403 on another user's coin (same ownership-check pattern as 3.3/3.4), via a private `assertOwned` helper. `DELETE` needs no explicit unlink step — the `CoinItem` row *is* the slot link, so deleting it frees the slot on its own; `SetSlot` is untouched. `CoinsService` returns a locally-declared `CoinItemView` (Prisma-native types: `@prisma/client`'s `Denomination`, `Prisma.Decimal`, `Date`) rather than importing `packages/shared`'s `CoinDto` directly — same convention as `SetListItem`/`UserSetSummary` in 3.3/3.4. This turned out to matter, not just be a style choice: Prisma's generated `Denomination` type is a plain string-literal union (`'Cent' | 'Nickel' | ...`), not a nominal TS `enum`, so `packages/shared`'s `Denomination` enum members are assignable *into* Prisma fields (create/update `data`) but Prisma's return values are **not** assignable back into a variable typed with the shared enum without a cast — relevant again for 4.3's `LinkingService`, which will need to compare/return denominations across both type worlds. `PATCH` relies on Prisma's own "`undefined` in `data` = leave unchanged, explicit `null` = clear" semantics, which lines up with `UpdateCoinDto` (`PartialType` + `@IsOptional()`, which class-validator treats as skip-on-`null`-too) with no extra service-layer branching needed. Verified live against the seeded Neon dev DB: unauthenticated `GET /coins` → 401; `POST` with `mintMark: "P"` stored/returned as `null`; `purchasePrice` round-trips as a numeric string (Prisma's `Decimal` has a `toJSON` returning a string, so no manual serialization needed); `PATCH` with `grade: null` cleared the field while leaving `year`/`mintMark` untouched; a second user got 403 on `PATCH`/`DELETE` of the first user's coin; `DELETE` of a bogus id → 404; owner `DELETE` → 204 and the coin dropped from the list. `LinkingService` (4.3) is done: `apps/api/src/coins/linking` — a `LinkingModule` (providers/exports `LinkingService`) imported by `CoinsModule`, per SD D2/D3 it's the sole implementor of the match rule (nothing else touches `slotId` yet — that arrives with 4.4's link/unlink endpoints). `suggest(userId, coin, excludeCoinId?)` is one `setSlot.findMany` joining `CollectionSet` (denomination match) → `UserSet` (`some: { userId }`, i.e. active/pursued sets only, never a set the caller hasn't activated) with a filtered `coinItems` include (`{ userId }`, minus `excludeCoinId` when re-suggesting for a coin being edited) to derive `currentlyLinkedCoinId`; `mintMark: coin.mintMark ?? null` is the null-safe equality the PRD §6.3 convention requires (confirmed live: a `"P"`-normalized Philadelphia 1909 cent matches its `null`-mint slot, not a separate "no match" case). `CoinsService.create`/`update` call `suggest` and wrap the result in a locally-declared `CoinMutationResponse` (Prisma-native `CoinItemView` + `packages/shared`'s `SlotSuggestion` — the latter is safe to import directly since it has no enum fields, unlike the `CoinItemView`/`CoinDto` split noted under 4.2). `POST /coins` always returns `CoinMutationResponse`; `PATCH /coins/:id` returns the bare `CoinItemView` when the edit didn't touch denomination/year/mintMark, and only wraps into `CoinMutationResponse` (recomputing suggestions, with the coin's own id excluded) when one of those three actually changed — matching SD §4's "`POST` (and `PATCH` when match key changed) → `CoinMutationResponse`" line precisely instead of always returning the wrapper. Verified live against the seeded Neon dev DB: a `mintMark: "P"` 1909 cent surfaced both the plain-1909 and VDB Philadelphia slots; a 1909-S cent surfaced both S and S-VDB (the key-date pair); a Morgan Dollar coin returned `suggestions: []` because that user had only activated Lincoln Cents (active-sets-only scoping, not just denomination+year+mint); a `PATCH` touching only `notes` returned the bare coin (no `suggestions` key at all), while a `PATCH` changing `year` returned a fresh `CoinMutationResponse` including the 1955 Doubled Die key-date slot. Test data cleaned up afterward. Hit the two known local-dev gotchas already documented below while verifying this — leftover `start:dev` watchers stomping a shared `dist/` (killed them, ran the compiled `dist/main.js` directly instead) and a stale `tsconfig.build.tsbuildinfo` silently no-op'ing the build (deleted it) — included here since they recurred, not because the fixes changed. Link/unlink endpoints (4.4) are done: `POST /coins/:id/link` and `POST /coins/:id/unlink`, both added to `LinkingService` rather than `CoinsService` — per SD D3 it's the sole writer of `CoinItem.slotId`, so `CoinsController` now injects `LinkingService` directly (`LinkingModule` is already imported by `CoinsModule`, so no new module wiring was needed) alongside `CoinsService`. `link(userId, coinId, slotId)` is one `prisma.$transaction` callback: fetch+check the coin (404 unknown, 403 foreign), fetch+check the slot including a `userSets: { where: { userId } }` filter to confirm the caller has actually activated its set (404 unknown slot, 403 un-activated set — same two-tier 404-then-403 shape as 3.3's `activate`), `findFirst` the current occupant (if any) to capture its id for the response, `updateMany` to null out that occupant's `slotId` (safe against 0-or-1 matches — this is what makes displacement-before-link the actual mechanism, not the `(userId, slotId)` unique constraint, which never fires because nothing is ever double-occupying by the time the final `update` runs), then `update` the target coin's `slotId` and return `{ coin, replacedCoinId }`. `unlink(userId, coinId)` is a plain ownership-checked `update` (no transaction needed — single write, nothing to displace) and is idempotent on an already-unlinked coin. Verified live against the seeded Neon dev DB with two throwaway users: linking coin A to a slot, then linking a second coin B to the *same* slot returned `replacedCoinId` = A's id and confirmed A's `slotId` was nulled out in a follow-up `GET /coins`; unlinking B then relinking it to the same slot returned `replacedCoinId: null` (no occupant to displace); a second user got 403 linking the first user's coin; a coin owner with no activated sets got 403 linking into a real slot (not just a foreign-coin case — the "pursued set" check is a separate assertion, not implied by coin ownership); bogus slot id → 404 "Slot not found"; bogus coin id → 404 "Coin not found"; non-UUID `slotId` in the request body → 400 from `class-validator`'s `@IsUUID()` on `LinkCoinDto`. All test users/coins/userSets removed afterward. E2e tests for 4.3/4.4 (4.5) are done: `apps/api/test/coins.e2e-spec.ts` activates the seeded Lincoln Wheat Cents template and covers three cases against real slot rows — a `mintMark: "P"` 1910 cent (a year with a single Philadelphia slot and no VDB variant, chosen to keep the match unambiguous) normalizes to `null` and matches exactly one suggestion; a 1909-S cent returns both the plain and VDB 1909-S slots (`isKeyDate: true` on both); and linking two coins to the same slot in sequence returns `replacedCoinId: null` then the first coin's id, with a follow-up `prisma.coinItem.findUniqueOrThrow` confirming the displaced coin's `slotId` actually went back to `null` (not just trusting the response body). Writing this test surfaced a **pre-existing, previously-undetected break in the e2e Jest config**, not something this task introduced: `packages/shared`'s `package.json` has `"type": "module"`, so once `CoinsModule`'s DTOs started importing `@coin-collector/shared` (back in 4.1/4.2), every e2e spec that boots `AppModule` — including `auth.e2e-spec.ts` and `user-sets.e2e-spec.ts`, both previously marked done — silently started failing at the Jest level (`SyntaxError: Unexpected token 'export'`). It went unnoticed because 4.2–4.4's own verification was done via live `curl` against the dev server, never `pnpm --filter api test:e2e`. Root cause: TypeScript's `NodeNext` module setting (from the shared `tsconfig.base.json`) emits real ESM for any file under a `"type": "module"` package regardless of what the *consumer* is — that's correct for production (Node 22's `require(esm)` support is why the live API never had a problem) but Jest's CommonJS-only runtime can't execute that output at all, and no amount of `ts-jest` config overrides (`isolatedModules`, inline `tsconfig: { module: "CommonJS" }`) actually changes the emitted syntax, because `ts-jest` resolves the module kind from the discovered project config keyed off the file's own package, not the override. Fixed in `apps/api/test/jest-e2e.json` with a dedicated transform entry routing anything under `packages/shared/src/*.ts` through a small custom transformer (`apps/api/test/support/shared-esm-transformer.js`, a direct `ts.transpileModule` call forcing `module: CommonJS`) instead of the default `ts-jest` entry, plus `moduleNameMapper` entries pointing `@coin-collector/shared` and its two internal relative imports (`./enums.js`, `./contracts.js`) at the package's TS sources rather than its built ESM `dist` — test-only, the real build/deploy path is untouched. All four e2e suites (`health`, `auth`, `user-sets`, `coins`) pass together after this fix; re-run `pnpm --filter api test:e2e` after any future change to `packages/shared` or its consumers to make sure this doesn't regress again. Gap endpoint (4.6) is done: `GET /user-sets/:id/gap` added to the existing `UserSetsController`/`UserSetsService` (no new module). `getGapView(userId, userSetId)` fetches the `UserSet` (404 unknown, 403 foreign — same two-check shape as `remove`), then one `setSlot.findMany` on `{ setId: userSet.setId }` with a `coinItems` include filtered to `{ userId }` and `orderBy: { sortOrder: 'asc' }` (SD D4 — a single query, no per-slot loop); the `(userId, slotId)` unique constraint from 1.3 guarantees that filtered include is 0-or-1 rows, so `slot.coinItems[0]` is a safe, non-defensive read. `GapViewResponse`/`GapSlot` are declared locally in `user-sets.service.ts` as a Prisma-native mirror of `packages/shared`'s contract types, same reasoning as `CoinItemView` vs. `CoinDto` under 4.2 (Prisma's `Denomination` union isn't assignable back into the shared enum) — `UserSetSummary` in the same file already followed this pattern. Verified live against the seeded Neon dev DB: activated Lincoln Wheat Cents on a throwaway user returned `totalSlots: 142, ownedSlots: 0`, all 142 slots `linkedCoin: null`, ordered by `sortOrder` starting at the 1909 VDB slot; adding a 1909 Philadelphia cent and linking it to its slot flipped that slot's `ownedSlots` count to 1 and populated `linkedCoin: { coinId, grade, acquiredDate }` for exactly that slot, all others unchanged; unauthenticated → 401; bogus user-set id → 404 "User set not found"; a second user hitting the first user's user-set id → 403 "Cannot view a pursuit you do not own". Test user/coin/userSet removed afterward. Frontend scaffold (5.1) is done: `apps/web` via `create-next-app` — App Router, TypeScript, Tailwind CSS v4, `src/` dir, `@/*` import alias; demo boilerplate (marketing homepage, `next.svg`/`vercel.svg`/etc. under `public/`) pruned to a one-line placeholder page and generic metadata. Scaffolded standalone via `pnpm dlx create-next-app`, which — unasked — writes its own `pnpm-lock.yaml`/`pnpm-workspace.yaml`/`node_modules` and its own `eslint.config.mjs` (flat config via `eslint-config-next`) into `apps/web`, none of which belong in a package that's supposed to join the root pnpm workspace; all four were deleted and `apps/web` re-installed through the root `pnpm install` instead, same as `apps/api`/`packages/shared`. Deliberately **not** wired to `eslint-config-next`, despite that being Next's own recommended lint setup: its bundled `eslint-plugin-react@7.37.5` peer-caps at `eslint: ^9.7` and throws (`contextOrFilename.getFilename is not a function`) under this repo's root `eslint@^10.6.0` on every `.tsx`/`.ts` file, including `next.config.ts` itself — confirmed via `npm view eslint-plugin-react peerDependencies`, not a fluke of this repo's config. Downgrading root `eslint` just to accommodate a plugin that hasn't caught up yet was judged worse than the alternative, so `apps/web` instead lints under the same root `tseslint.configs.recommended` (+ `eslint-config-prettier`) as every other workspace package — plain TS rules, no React/Next-specific ones (`react-hooks`, `jsx-a11y`, etc.) for now; revisit once `eslint-plugin-react` (or its replacement) supports ESLint 10. `eslint.config.mjs`'s root `ignores` gained `**/next-env.d.ts` (Next's own generated ambient-types file, previously irrelevant since no `apps/*` package emitted one). `pnpm-workspace.yaml`'s `allowBuilds` gained `sharp: true` — it's `next`'s own `optionalDependencies` entry (native image-processing lib backing `next/image` optimization, not a telemetry script like `@scarf/scarf`), so pnpm's ignored-build-scripts guard needed an explicit yes, same treatment as `bcrypt`/`prisma` in 1.x. `apps/web/package.json`'s generated `lint` script and `eslint`/`eslint-config-next` deps were removed (no per-package lint script anywhere else in the workspace — `pnpm lint` from root is the only entry point). `pnpm --filter web build` and a manual `pnpm --filter web dev` + `curl localhost:3000` both verified clean (Turbopack build, 200 response rendering the placeholder text) before `.next/` was deleted again (gitignored, shouldn't linger in a scaffold-only commit). API client + TanStack Query (5.2) is done: four files under `apps/web/src` — `lib/auth-token.ts` (`getStoredToken`/`setStoredToken`/`clearStoredToken`, `localStorage` key `ccc.accessToken`, SSR-guarded via `typeof window === 'undefined'`), `lib/api-client.ts` (`apiFetch<T>(path, init)` — generic, no resource-specific methods; those arrive with each page in 5.5+), `lib/query-client.ts` (`createQueryClient()` factory), and `components/providers/query-provider.tsx` (`'use client'`, holds the `QueryClient` in `useState` so it survives re-renders but is never shared across requests), wired into `src/app/layout.tsx` wrapping `{children}`. `apiFetch` attaches `Authorization: Bearer` from `localStorage` and a 75s-per-attempt `AbortController` timeout (SD §6's accepted 30–60s Render cold-start window), retrying exactly once — only on `AbortError`/`TypeError` (the request never reached the API, the cold-start signature), never on an HTTP error status (the server already processed that request, so retrying could double a `POST`). A 401 response clears the stored token and hard-redirects to `/login` via `window.location.href` before throwing, satisfying the backlog's "redirect-to-login on 401" without needing router context inside a plain lib function. `QueryClient`'s own `retry` is set to `false` in `query-client.ts` specifically to avoid stacking a second ~75s wait on top of `apiFetch`'s own retry. `apps/web/.env` + `.env.example` added (`NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1` locally) — the SD §7 "`.env.example` at both app roots" line was still outstanding for `apps/web` since 5.1 predates any env var use. Verified: `pnpm lint`, `pnpm --filter web exec tsc --noEmit`, and `pnpm --filter web build` all clean; `pnpm --filter web dev` + `curl localhost:3000` returned 200 with `QueryProvider` present in the render tree and no console errors; a standalone Node script hitting the real (cold) Render `/health` endpoint through the same timeout/retry logic returned 200 in ~32s on the first attempt, confirming the 75s budget has real headroom against the documented cold-start window. No resource-specific query hooks yet (`useUserSets`, `useCoins`, etc.) — those land alongside the pages that need them starting at 5.5. Set catalog page (5.5) is done: three files — `apps/web/src/lib/sets-api.ts` (mirrors `apps/api`'s `SetListItem`/`ActivatedUserSet` locally, same convention as `auth-api.ts` — `packages/shared`'s `contracts.ts` intentionally doesn't cover `GET /sets`/`POST /sets/:id/activate`, only the non-obvious payload shapes per SD §4), `apps/web/src/lib/hooks/use-sets.ts` (`useTemplateSets` query + `useActivateSet` mutation — the app's first resource-specific TanStack Query hooks), and `apps/web/src/app/(app)/sets/page.tsx` (list grouped by `category`, matching the `orderBy: [{category},{name}]` `SetsService.findAll` already returns; per-row Activate button tracks pending/success via `activateMutation.variables === set.id` plus a local `activatedIds` set — `GET /sets` doesn't report whether the caller already activated a given template, so "Activated ✓" is this page's own optimistic memory, not server state, and resets on reload). This is the first page to import `@coin-collector/shared` directly (`Denomination`, for a `HalfDollar` → "Half Dollar" display formatter) — added as a real `workspace:*` dependency in `apps/web/package.json`, same as `apps/api`. Verified live: registered a throwaway user, confirmed `GET /sets?isTemplate=true`'s three seeded templates match the local `SetListItem` type field-for-field, activated one and confirmed idempotent re-activation returns the identical `UserSet` id, then loaded `/sets` against a running `next dev` server and confirmed a clean 200 with no console/server errors; test user and its `UserSet` row removed afterward. Hit two dev-environment snags, not new bugs: the `tsconfig.build.tsbuildinfo`-vs-missing-`dist` no-op described below recurred for `apps/api start:dev` and needed the same fix (delete the stale buildinfo file); and `apps/api`'s and `apps/web`'s dev servers both default to port 3000 — `next dev` auto-falls-back to 3001 when 3000 is taken, so start the API first if running both locally together. My Sets page (5.6) is done: three files — `apps/web/src/lib/user-sets-api.ts` (a thin `getUserSets` wrapper — unlike `sets-api.ts`/`auth-api.ts`, no locally-mirrored type was needed since `GET /user-sets`'s shape is exactly `packages/shared`'s `UserSetSummary`, imported directly), `apps/web/src/lib/hooks/use-user-sets.ts` (`useUserSets` query, `['user-sets']` key — the key later mutations from 5.9 will invalidate per SD §3), and `apps/web/src/app/(app)/my-sets/page.tsx` (route path per SD §3's route map, not `/user-sets`) — lists each pursued set with a client-derived `Math.round((owned/total)*100)` percentage (SD's "one rounding rule, one place" — this is that place) plus the raw `owned / total` count and an `isComplete` "Complete ✓" tag reusing the field already on `UserSetSummary`, each row linking to `/my-sets/[userSetId]` (the SD-mapped gap-view route, not yet built — that's 6.1). No global nav exists yet to reach this page from `/sets`; none was added here since it's not part of this task and no other page links between routes yet either. Verified live: activated the seeded Lincoln Wheat Cents template on a throwaway user, confirmed `GET /user-sets` returned `totalSlots: 142, ownedSlots: 0`, loaded `/my-sets` against a running `next dev` server and got a clean 200 with no console/server errors showing "0 / 142" and "0%"; `pnpm --filter web exec tsc --noEmit`, `pnpm lint`, and `pnpm --filter web build` all clean (new route shows as a static page in the build output); test user and its `UserSet` row removed afterward. Coin list page (5.7) is done: three files — `apps/web/src/lib/coins-api.ts` (`getCoins`/`deleteCoin`; unlike `sets-api.ts`/`user-sets-api.ts`, no locally-mirrored type needed since `GET /coins`'s `CoinItemView` shape is field-for-field `packages/shared`'s `CoinDto`, imported directly), `apps/web/src/lib/hooks/use-coins.ts` (`useCoins` query + `useDeleteCoin` mutation invalidating `['coins']`), and `apps/web/src/app/(app)/coins/page.tsx` — each row shows year-mint + denomination, grade (or "Ungraded"), and a "Linked" tag when `slotId` is set; delete is a two-step inline confirm (per-row `confirmingId` state swaps the Delete button for Confirm/Cancel, no native `confirm()` dialog) rather than immediate deletion. No global nav links to `/coins` yet, same as 5.5/5.6 — still no nav component in the app. Verified live: registered a throwaway user, added two coins via the API, confirmed `GET /coins` returned both, `DELETE /coins/:id` returned 204 and the follow-up list reflected the removal, loaded `/coins` against a running `next dev` server (port 3001, since `apps/api`'s dev server held 3000) and got a clean 200 with no server errors; `pnpm --filter web exec tsc --noEmit`, `pnpm lint`, and `pnpm --filter web build` all clean. Test user and coins removed afterward. Coin add/edit form (5.8) is done: `apps/web/src/components/coins/coin-form.tsx` (a shared `'use client'` form used by both the add and edit routes), `apps/web/src/app/(app)/coins/new/page.tsx`, and `apps/web/src/app/(app)/coins/[id]/edit/page.tsx` — the app's first dynamic route segment. The edit page has no `GET /coins/:id` to call (none exists), so it seeds the form from the already-fetched `useCoins()` list, matched by the route's `id` param, rather than adding a new backend endpoint for this. `CoinForm` sends every field on every submit (never `undefined`) — "None" mint mark and "Ungraded" grade map to explicit `null`, matching `CreateCoinDto`/`UpdateCoinDto`'s `IsOptional` (skip-on-null) semantics from 4.1, so PATCH's own undefined-vs-null distinction never comes into play here. Both `coins-api.ts`'s `createCoin`/`updateCoin` and `use-coins.ts`'s new `useCreateCoin`/`useUpdateCoin` invalidate `['coins']` on success; the 5.9 suggestions panel isn't wired yet, so both mutations just redirect to `/coins` on success today, ignoring `CoinMutationResponse.suggestions` for now (by backlog design — 5.8 and 5.9 are deliberately separate tasks). `coins/page.tsx` gained an "Add Coin" button and a per-row "Edit" link to reach the new routes; nothing else linked into `/coins` before this. Verified end-to-end with Playwright against real dev servers (register → add coin → list shows it → edit → save → list reflects the change → delete → list empty), zero console errors. That verification run surfaced a **real, pre-existing backend bug**, not something this task introduced: `POST`/`PATCH /coins` 500'd whenever `acquiredDate` was a bare `"YYYY-MM-DD"` string — exactly what an `<input type="date">` sends, and still valid per `@IsDateString()` — because Prisma's `DateTime` field rejects a date-only ISO string at the query-engine level, and nothing translated that into a 400. It went undetected until now because no prior task's verification (curl or otherwise) had exercised `acquiredDate` as a date-only string on write. Fixed with a `@Transform` on `CreateCoinDto.acquiredDate` (inherited by `UpdateCoinDto` via `PartialType`) that pads a bare date to a full UTC midnight ISO datetime before validation — same "normalize at the API boundary" pattern 4.1 already used for mint-mark `"P"`/`""` → `null`. Confirmed live via curl before and after: 500 → 200/201, with the date round-tripping correctly (`"2026-07-01"` in, `"2026-07-01T00:00:00.000Z"` out) on both create and update, including a follow-up PATCH clearing it back to `null`. Also worth noting for future local verification: this session's Playwright check needed the API and web dev servers to actually agree on ports — `main.ts`'s CORS allowlist is `CORS_ORIGIN` + a hardcoded `http://localhost:3000` only, so if the API claims port 3000 (leaving `next dev` to fall back to 3001, per the already-documented port clash above), every browser-side `fetch` from the web app 3001 origin is silently CORS-blocked (a `TypeError`, not an HTTP error — `apiFetch` retries it once as a cold-start signature, then surfaces a generic "something went wrong" with no server-side trace). The 5.5–5.7 "clean load, no console errors" verifications never actually caught this because none of them completed a mutating fetch from the fallback port; a plain unauthenticated page load doesn't hit it since it redirects to `/login` before any `fetch` fires. Worked around for this session by running the API on `PORT=4000` and the web app on the freed port 3000 (`http://localhost:3000` being the one CORS origin that's always allowed) — not a code change, just how the two dev servers need to be started together locally until `CORS_ORIGIN` is set to also cover 3001 or a proxy is introduced. Auto-suggest panel (5.9) is done: `apps/web/src/components/coins/suggestion-panel.tsx` (a shared `'use client'` panel used by both `coins/new` and `coins/[id]/edit`), plus `linkCoin` added to `coins-api.ts` and a new `useLinkCoin` hook in `use-coins.ts`. Both form pages now branch on `onSuccess`: 0 suggestions redirects straight to `/coins` as before, ≥1 swaps the form out for the panel instead (`mutationResult` state) — `create`'s response is always `CoinMutationResponse`, but `update`'s is `CoinDto | CoinMutationResponse`, so the edit page narrows with `'suggestions' in result` before branching, since PATCH only wraps the response when denomination/year/mintMark actually changed (4.3). The panel itself is dumb about *which* case it's in beyond suggestion count: 1 suggestion renders a single confirm button (label switches to "Replace current link" when `currentlyLinkedCoinId` is set), >1 renders a radio-button picker with a shared "Confirm link" button; either path calls `useLinkCoin`, and a "Not now" link skips straight to `/coins` without linking. `useLinkCoin`'s `onSuccess` invalidates `['coins']`, `['user-sets']`, and `['gap']` — the last as a bare one-element key, not `['gap', userSetId]` as SD §3 literally reads, because the suggestion payload (`SlotSuggestion`) carries `slotId`/`setName`/`slotLabel`, never the owning `userSetId`; TanStack Query's `invalidateQueries` matches by key *prefix*, so `['gap']` still invalidates every `['gap', userSetId]` query once 6.1 introduces one, without needing to thread that id through. Verified via `pnpm --filter web exec tsc --noEmit`, `pnpm lint`, and `pnpm --filter web build` (all clean), plus a live curl-driven run against the real Neon dev DB confirming the exact contract the panel branches on: a 1909-S cent returned both its plain and VDB slot suggestions (`isKeyDate: true` on both, the picker case); linking it to one, then adding a second 1909-S cent, surfaced `currentlyLinkedCoinId` set on that same slot in the fresh suggestions list (the "Replace current link" case) while the other slot's stayed `null`. No browser was available in this environment to click through the actual panel UI, so the component's render logic (button/radio branching, label text) is verified by code review and the clean typecheck/build, not by an observed click-through — worth an actual browser pass before calling 5.9 fully done end-to-end. Test user/coins/userSet cleaned up afterward via a throwaway Prisma script (no user-delete endpoint exists to do it through the API). Note: `apps/api` pins `typescript@^5.7.3` (not the root's `^6.0.3`) — `@nestjs/cli@11`'s compiler silently produces no build output under TypeScript 6 (version mismatch between its own bundled `typescript` and the loaded project one); root tooling (ESLint/Prettier) and `packages/shared` are unaffected and stay on 6.0.3. Separately, if `apps/api/tsconfig.build.tsbuildinfo` (incremental build cache, gitignored) goes stale relative to a wiped `dist/` — e.g. two `start:dev` watchers left running against the same `dist` — `tsc`/`nest build` can silently no-op and emit nothing; delete that file to force a full rebuild if `dist/main` goes missing after a build that reported success. Also note: `apps/api` pins `prisma`/`@prisma/client@^6` — Prisma 7 removes the schema-file `datasource { url = env(...) }` form entirely (moves connection config to `prisma.config.ts` + driver adapters), which the PRD §6.2 schema sketch assumes; don't upgrade to 7 without a deliberate migration of that config. Also note: root `package.json` pins `"engines": { "node": ">=22.0.0" }` and pnpm 11 enforces it — the machine's default `node` (via nvm) is v18, so any `pnpm`/`prisma` command from the repo root needs `nvm use 22` (or newer) first or it fails outright before touching your code. Available today, run from the repo root (with Node ≥22 active):

- `pnpm lint` / `pnpm lint:fix` — ESLint across the workspace
- `pnpm format` / `pnpm format:check` — Prettier across the workspace
- `pnpm --filter api start:dev` — NestJS dev server (watch mode), `GET http://localhost:3000/api/v1/health` once up, Swagger UI at `http://localhost:3000/api/docs`
- `pnpm --filter api build` — production build
- `pnpm --filter api test` / `pnpm --filter api test:e2e` — unit / e2e tests
- `pnpm --filter @coin-collector/shared build` — compile the shared enums/contracts package to `dist`
- `pnpm --filter api exec prisma generate` — regenerate the Prisma client from `schema.prisma`
- `pnpm --filter api exec prisma migrate dev --name <name>` — create + apply a new migration against the Neon `DATABASE_URL`
- `pnpm --filter api exec prisma migrate deploy` — apply pending migrations without prompting (CI/prod path)
- `pnpm --filter api exec prisma db seed` — walk `seed/templates/*.json`, upsert into `CollectionSet`/`SetSlot` (also runs automatically after `prisma migrate reset`)
- `pnpm --filter web dev` — Next.js dev server, `http://localhost:3000` (no real pages yet — placeholder text only)
- `pnpm --filter web build` — production build

Update this section again as each further real command appears (seed script → `seed`; Next.js scaffold → frontend `dev`), per backlog item 0.5.

## What this project is

Coin Collector Companion — a personal collection tracker whose core differentiator is the **gap view**: given a canonical "set" (e.g. Lincoln Wheat Cents 1909–1958, all mints), show which slots are owned vs. missing, with completion % and key-date flags. Everything else (coin CRUD, photos, dashboards) is secondary to that one feature. Read the full PRD at [docs/prd.md](docs/prd.md) before making scope decisions — it resolves several inconsistencies explicitly and explains *why*, not just *what*.

## Architecture (as planned)

Frontend (Next.js, App Router) and backend (NestJS) run as **separate services at runtime** — not a Next.js app using API routes as the backend — this is deliberate, to force a real API boundary (DTOs, validation, guards) for portfolio purposes. Do not collapse them into one running service.

- **Repo layout:** single monorepo (pnpm/npm workspaces: `apps/web`, `apps/api`, `packages/shared` for DTO/type sharing) — see [docs/prd.md](docs/prd.md) §8.1. This is orthogonal to the runtime-separation rule above: one repo, two independently built/deployed services.
- **DB:** PostgreSQL via Prisma (decided — do not reintroduce a TypeORM debate).
- **Auth:** hand-rolled Passport + JWT + bcrypt in Nest, email/password only. No social login, no outsourced auth BaaS.
- **Images:** out of scope until Phase 3 (Cloudflare R2, presigned uploads). Don't build image upload infrastructure earlier than that — the `imageUrls` column exists from Phase 1 (empty array, not nullable) but is unused until then.
- **Hosting (when deploying):** Vercel (frontend), Render (backend), Neon (DB). Free tier throughout; Render cold-starts after 15 min idle — this is an accepted, documented tradeoff, not a bug to fix.

## Data model (high level — see PRD §6 for full detail)

- `User` — id, email, password hash.
- `CollectionSet` — a set definition (template or user-created). `denomination` (shared enum from `packages/shared` — never free text; it's half the auto-suggest match key), `isTemplate` flag; `ownerId` is null for templates, set for user-created sets.
- `SetSlot` — one expected item within a `CollectionSet` (year, mint mark, label, sort order, `isKeyDate`).
- `UserSet` — joins a `User` to a `CollectionSet` they're pursuing. **Every pursued set gets a `UserSet` row, including the user's own custom sets** — don't special-case "template vs. mine" in pursuit/progress logic; `CollectionSet.ownerId` answers *who may edit the definition*, `UserSet` answers *who is collecting against it*.
- `CoinItem` — belongs to a `User`; denomination (same shared enum), year, mint mark, country, grade, purchase price (USD-only in v1, `Decimal`), notes, acquired date, `imageUrls` (empty until Phase 3), optional FK to a `SetSlot`.

Mint-mark convention (decided, PRD §6.3): a coin/slot struck with **no mint mark stores `null`, never `"P"`** — in template JSON, the coin form, and API normalization alike. Matching is exact (`null` matches `null`); violating this breaks auto-suggest for Philadelphia issues, the most common coins in most US series.

Key invariant: multiple `CoinItem`s may exist for the same year/mint (duplicates are normal), but **at most one `CoinItem` may be linked to a given `SetSlot` per user** — enforce with a unique constraint on `(userId, slotId)`. Relinking a slot to a different coin replaces the link rather than erroring (the unlink+link pair must run in a single transaction); the old coin stays in the collection, just unlinked.

Gap computation: for a given `UserSet`, gap = the `SetSlot`s with no linked `CoinItem` for that user. Keep this as a simple query — it's the core product, don't over-engineer it.

## Scope discipline

The PRD is phased on purpose (Phase 1 = gap view only, ~1 week; Phase 2 = custom set builder + polish; Phase 3 = photos; Phase 4 = stretch, design-for-don't-build). When asked to add a feature, check which phase it belongs to before building it:

- **Two-state slots only** (owned/missing) in Phase 1+2. "Owned but want a better example" is a Phase 4 want-list concept — don't add a third slot state to smuggle it in early.
- **No photos before Phase 3.** Don't add upload UI, storage config, or image processing before then.
- Auto-suggest for slot linking (matching a newly added coin against open slots by denomination + year + mint mark) ships in **Phase 1**, not later — it's what keeps the gap view trustworthy, not a nice-to-have.
- Seeded set templates live as **versioned JSON files in the repo**, loaded via a seed script — not hardcoded in migrations, not fetched from an external API. The seed script upserts slots on the natural key `(setId, year, mintMark, label)` — **never delete-and-recreate slots wholesale**, since `CoinItem.slotId` points at slot rows and recreating them severs every user's links on each template correction.
- The Phase 1 acceptance test is the scope firewall: register → activate Lincoln Cents template → add 5 real coins → auto-suggest links them → gap view shows correct owned/missing/completion %. Anything not needed for that path is a later phase.
