// v2 enums/contracts pending — see docs/system-design_v2.md §3–§4 (catalog query DTOs,
// GapViewResponse-equivalent, canonical/public set shapes). The v1 exports (Denomination,
// Grade, CoinDto, GapViewResponse, SlotSuggestion, etc.) were removed as part of the v2 prep
// pass; re-populate this package as the real v2 DTOs/enums are authored, and keep it the
// single source of truth shared by both apps (CLAUDE.md).
export {};
